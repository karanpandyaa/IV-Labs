hrs = input("Enter Hours:")
h = float(hrs)
rate=input("Enter Rate per hour")
rph=float(rate)
if h<=40:
   pay=h*rph
   print(pay)
else:
   pay=40*rph
   pay=pay+((h-40)*1.5*rph)
   print(pay)
    
    
